---
layout: default
title: My Filtered Archive
---

{% assign dir_name = p.dir | split: "/" | last %}

# {{ dir_name }} With Cheese

<ul>
  {% comment %}
    1. Get all pages in the site.
    2. Filter to keep only pages in the current directory.
    3. Sort alphabetically by path, then reverse it (Z to A / Newest to Oldest).
  {% endcomment %}
  
  {% assign current_dir = page.dir %}
  {% assign all_pages = site.pages | sort: "path" | reverse %}

  {% for p in all_pages %}
    {% if p.dir == current_dir %}
      
      {% comment %}
        Extract just the filename from the full path to check for "20".
        Example path: "notes/2026-b.md" -> split by "/" -> last item is "2026-b.md".
      {% endcomment %}
      {% assign filename = p.path | split: "/" | last %}
      {% assign file_start = filename | slice: 0, 2 %}

      {% if file_start == "20" and p.url != page.url %}
        <li>
          <a href="{{ p.url | relative_url }}">
            Reunion {{ p.title | default: filename }}
          </a>
        </li>
      {% endif %}

    {% endif %}
  {% endfor %}
</ul>




  {% comment %}
## El Diario De Greg - Jeff Kinney

* [Reunion July 26, 2026](20260726_notas.md) 
  {% endcomment %}

***

El libro se enquentra [aqui](https://archive.org/details/eldiariodegreg)

*This file is autogenerated.  Do not edit.*

